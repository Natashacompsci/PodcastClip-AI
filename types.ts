
export interface Clip {
  title: string;
  summary: string;
  transcript: string;
  timestamp: string;
  quote: string;
  hashtags: string[];
  platform: string;
  speaker: string;
  cardImageUrl?: string;
}